SYSTEM = RuO2 surface calculation

# start parameters
  ISTART = 0        #start job from scratch
  LWAVE  = .FALSE.  do not write WAVECAR file
  #LCHARG = .FALSE.  do not write CHGCAR file
  #LVTOT  = .FALSE.  do not write LOCPOT file
  LVHAR  = .TRUE.
  LSCALAPACK = .FALSE.
  ALGO = FAST

  NPAR = 4          # parallelization
 
# electronic relaxation
  ENCUT = 600.00 eV  #energy cut-off for the calculation
  #PREC  = Accurate   #normal precision
  #LREAL = .FALSE.    #real space projection
  LREAL = Auto       #faster calculation to make up for large cell size
  #EDIFF = 10e-6
  NELM  = 150        #max # scf cycles
  #ISMEAR = 1        #Methfessel-Paxton order N (?), these are the default values
  #SIGMA = 0.1
  ISPIN = 2
  MAGMOM = 16*0 32*0 20*1

# ionic relaxation

  EDIFFG = -0.01
  NSW = 2000
  #NSW = 150
  POTIM = 1.0
  IBRION = 2
  ISIF = 2           # isif=7 -> only change cell volume (3 is relax everything)
  #ISYM = 2
  #IVDW=1

# Dipole corrections due to asymmetry
  IDIPOL = 3
  LDIPOL = .TRUE.

#  EDIFFG = -0.01
#  NSW = 200
#  IBRION = 2
#  ISIF = 7
  
DOS related values:
    ISMEAR = 0  ; SIGMA = 0.10
    NEDOS  = 1000
    EMIN   = -15
    EMAX   = 15
    LORBIT = 11


